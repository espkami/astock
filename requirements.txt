# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
aiosqlite==0.20.0

# Scheduling
apscheduler==3.10.4

# News collection
feedparser==6.0.11
httpx==0.28.0

# Stock data
# akshare 通过 Dockerfile --no-deps 单独安装（规避 py-mini-racer arm64 无 wheel 问题）
# 以下是 akshare 的全部依赖，在此显式声明确保干净环境可用
akracer==0.0.14
beautifulsoup4==4.14.3
curl_cffi==0.15.0
decorator==5.2.1
html5lib==1.1
jsonpath==0.82.2
lxml==6.1.0
openpyxl==3.1.5
tabulate==0.10.0
xlrd==2.0.2
tushare==1.4.29

# LLM SDKs
anthropic==0.40.0
openai==1.57.0

# Vector / ML
scikit-learn==1.5.2
numpy>=2.1.0,<3.0.0

# Utilities
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
loguru==0.7.2

# Auth
python-jose[cryptography]==3.3.0
bcrypt>=4.0.0

# Debug (支持 DEBUG=1 模式)
debugpy==1.8.9

# 新闻正文回源补全（content_fetcher.py）
trafilatura==1.12.2
newspaper3k==0.2.8
lxml_html_clean==0.4.1     # newspaper3k 在新版 lxml 下的必要补丁
requests==2.32.3

# 可选：JS渲染/强反爬时才需要
# playwright（需额外执行 playwright install chromium）
